# Evaluation Tasks

This directory contains example graded tasks for the `email_triage_rl` environment.

Each task file defines:

- a small inline dataset of email triage examples
- a task factory
- a grader/scorer that checks structured JSON predictions

The validator requirement is satisfied by providing at least three tasks with graders.
